import { style } from '@vanilla-extract/css';

export const container = style({
  marginTop: '100px',
  textAlign: 'center',
});

export const icon = style({
  width: '15px',
  height: '15px',
  margin: '0 auto',
  whiteSpace: 'nowrap',
  textAlign: 'center',
});
